---
- src: "nr-otel-collector_{version}_windows_amd64.zip"
  uploads:
    - type: file
      dest: "{dest_prefix}binaries/windows/amd64/{src}"

#########################
  # UPLOAD CHECKSUMS #
#########################

- src: "nr-otel-collector_{version}_windows_amd64.zip.sum"
  uploads:
    - type: file
      dest: "{dest_prefix}binaries/windows/amd64/{src}"
